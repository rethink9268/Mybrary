<h2 class="page_header"><%= author.name%></h2>
<div class="btn_row">
  <a class="btn btn-primary" href="/authors/<%=author.id%>/edit">Edit</a>
  <%- include('../partials/deleteForm', {url: `/authors/${author.id}`})%>
</div>
<% if(booksByAuthor.length > 0) { %>
<h2 class="page_header">Books By Author</h2>
<%-include('../partials/bookGrid', {books: booksByAuthor, large: false})%> <%
}%>
